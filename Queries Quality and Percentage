#Problem Link - https://leetcode.com/problems/queries-quality-and-percentage/?envType=study-plan-v2&envId=top-sql-50


Select query_name, round(avg(rating / position), 2) as quality, round(100*AVG(rating < 3), 2) as poor_query_percentage
from Queries
where query_name is not null
group by query_name
